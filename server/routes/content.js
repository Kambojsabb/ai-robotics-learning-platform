const r=require('express').Router();const c=require('../controllers/content');const {authenticate,authorize}=require('../middleware/auth');r.use(authenticate);r.get('/stats',authorize('ADMIN','TEACHER'),c.stats);r.get('/',c.list);r.get('/:id',c.get);r.post('/',authorize('ADMIN','TEACHER'),c.create);r.put('/:id',authorize('ADMIN','TEACHER'),c.update);r.delete('/:id',authorize('ADMIN','TEACHER'),c.remove);module.exports=r;

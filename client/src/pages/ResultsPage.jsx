import {useEffect,useState} from 'react';import api from '../services/api';import {Frame} from './Dashboard';import {PageTitle,Card,Empty} from '../components/UI';import {useAuth} from '../context/AuthContext';
export default function ResultsPage(){const {user}=useAuth();const [items,setItems]=useState([]);useEffect(()=>{api.get('/tests/results').then(r=>setItems(r.data.items))},[]);return <Frame role={user.role}><PageTitle eyebrow="RESULTS" title="Test Results" description={user.role==='STUDENT'?'Your own results':'Student performance'}/><Card className="overflow-hidden">{items.length?<div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-white/5 text-slate-400"><tr><th className="p-4">Student</th><th>Student ID</th><th>Test</th><th>Score</th><th>Percentage</th><th>Date</th></tr></thead><tbody>{items.map(x=><tr key={x._id} className="border-t border-white/5 text-slate-300"><td className="p-4">{x.student?.name}</td><td>{x.student?.studentId||'—'}</td><td>{x.test?.title}</td><td>{x.score}/{x.totalMarks}</td><td className="font-bold text-cyan-300">{x.percentage}%</td><td>{new Date(x.submittedAt).toLocaleDateString()}</td></tr>)}</tbody></table></div>:<Empty text="No results yet."/>}</Card></Frame>}
